# CutCap PRD

## 1. 문서 정보

| 항목 | 내용 |
|---|---|
| 제품명 | CutCap |
| 버전 | 1.2 |
| 작성일 | 2026-09-21 (v1.2 CutCap 개명) |
| 문서 상태 | 개발 착수 가능 |
| 한 문장 정의 | 설치도 로그인도 없이 브라우저에서 영상을 자르고 이어 붙여, 원하는 용량과 화면비에 맞춰 내보내는 무료 웹 편집기 |

## 2. 문제와 목표

**현재 불편**

- 기존 영상 편집기는 설치·로그인·구독이 필요하거나, 무료판은 결과물에 워터마크를 남깁니다.
- "필요 없는 앞뒤를 잘라내고 몇 개를 이어 붙이는" 단순 작업에 전문 편집기는 과합니다.
- 제출 시스템의 용량 상한(예: 업무포털 20MB)을 맞추려면 별도 변환 도구를 다시 찾아야 합니다.
- 다수의 무료 웹 도구는 영상을 서버에 업로드하므로, 얼굴이 담긴 영상을 올리기 부담스럽습니다.

**해결할 일**

1. 영상을 올린 즉시 타임라인에서 자르고 이어 붙이게 한다.
2. 출력 용량·화면비·화질을 목적 기준으로 고르게 한다.
3. 영상을 기기 밖으로 내보내지 않는다.
4. 중단한 작업을 다시 이어서 할 수 있게 한다.

**성공 지표 (제안)**

| 지표 | 목표 | 측정 방법 |
|---|---|---|
| 첫 내보내기 도달률 | 영상을 올린 세션의 60% 이상이 내보내기 완료 | 클라이언트 내 익명 카운터(기기 로컬 집계, 외부 전송 없음) |
| 내보내기 실패율 | 5% 미만 | 실패 사유별 로컬 카운터 |
| 첫 컷 편집까지 걸린 시간 | 영상 선택 후 60초 이내 첫 분할 수행 | 로컬 타이머 |
| 목표 용량 준수율 | 목표 용량 지정 시 95% 이상이 목표 이하로 산출 | 내보내기 결과 크기 비교 |

## 3. 대상과 사용 상황

주 사용자는 제한하지 않습니다. 대표 시나리오 3가지를 기준으로 설계합니다.

| 시나리오 | 상황 | 요구되는 기능 |
|---|---|---|
| S-1 수업 녹화 정리 | 교사가 25분 수업 녹화에서 준비 구간과 쉬는 구간을 잘라내고 20MB 이하로 제출 | 분할, 구간 삭제, 목표 용량 인코딩 |
| S-2 학생 과제 제작 | 학생이 태블릿으로 찍은 영상 3개와 사진 2장을 이어 붙이고 앞에 제목용 빈 화면을 넣음 | 다중 소스, 이미지, 빈 화면, 순서 변경 |
| S-3 이어서 작업 | 학교에서 하던 편집을 집에서 이어감 | 자동 저장 복원, 프로젝트 파일 내보내기·불러오기 |

**사용 환경**

- 데스크톱: Chrome/Edge 최신, Safari 26 이상, Firefox 130 이상
- 모바일: Android Chrome/삼성인터넷 최신, iOS Safari 16.4 이상
- 네트워크: 최초 로딩 이후 오프라인에서도 편집·내보내기 가능(PWA)

## 4. 범위와 우선순위

**MVP 포함**

- 로컬 영상·이미지 불러오기, 다중 소스 타임라인
- 재생헤드 위치 분할, 클립 트림(길이 줄이기·되돌리기), 삭제, 순서 변경
- 빈 화면(단색) 삽입 및 길이 지정
- 클립별 음소거·볼륨
- 자막 직접 입력, 영상에 굽기, `.srt` 동시 내보내기
- 출력 설정: 화면비, 해상도, 화질, 목표 용량
- MP4 내보내기 및 저장
- 브라우저 자동 저장 + `.cutcap` 내보내기·불러오기
- 모바일 전용 세로 편집 레이아웃
- PWA(홈 화면 추가, 오프라인 실행)

**MVP 제외 (제외 이유 포함)**

| 제외 항목 | 이유 |
|---|---|
| 배경음악·오디오 믹싱 | 저작권 처리와 싱크 버그 위험이 크고, 컷편집이라는 핵심 가치와 분리됨 |
| 자동 음성인식 자막 | 서버 없이 브라우저에서 모델을 돌리면 수십~수백MB 다운로드가 필요하고 저사양 기기에서 실패 위험이 큼 |
| 텍스트 오버레이·스티커 | 자막과 달리 자유 배치·애니메이션이 필요해 범위가 급증 |
| 화면 전환 효과, 필터 | 컷편집 완결에 불필요 |
| 계정·클라우드 저장 | 서버를 두지 않기로 한 결정과 충돌 |
| WebM·GIF·오디오 추출 | 검증 경로 증가 |
| 서버 인코딩 | 운영비와 개인정보 위험 |

**후속 (2차 이후)**

자동 음성인식 자막, 배경음악 1트랙, 텍스트 오버레이, 속도 조절, 화면 전환, 서버 인코딩 옵션.

## 5. 사용자 흐름

**대표 경로 (F-1)**

시작 → 첫 화면에서 "영상 선택" 또는 드래그 → 타임라인에 클립 추가 → 재생헤드 이동 후 분할 →
불필요한 클립 삭제 → 필요 시 빈 화면·이미지 추가 → 순서 정리 → "내보내기" → 화면비·해상도·화질
또는 목표 용량 선택 → 진행률 표시 → 완료 → 파일 저장 → 계속 편집 또는 새 프로젝트

**분기·실패 흐름**

| 상황 | 동작 |
|---|---|
| 브라우저가 해당 코덱을 못 읽음 | 파일별로 "이 브라우저에서 열 수 없는 형식"이라 표시하고 나머지 파일은 정상 추가 |
| WebCodecs 미지원 | ffmpeg.wasm 경로로 자동 전환하고 "이 브라우저에서는 내보내기가 더 오래 걸립니다" 고지 |
| 저장공간 부족 | 자동 저장 전에 경고하고 프로젝트 파일 내보내기를 권유 |
| 내보내기 중 탭 이탈·새로고침 | 이탈 경고 표시, 복귀 시 프로젝트는 복원되나 내보내기는 처음부터 |
| 목표 용량 미달성 | 1회 재인코딩 후에도 초과하면 해상도 한 단계 낮춤을 제안 |
| 인코딩 중 메모리 부족 | 실패 사유와 함께 "해상도를 낮추거나 영상을 나눠 내보내기"를 안내 |
| 빈 프로젝트 | 첫 화면에 사용법 3단계와 "영상 선택" 버튼만 표시 |

## 6. 화면·UX 요구사항

**화면 구성**

| 화면 | 목적 | 주요 요소 |
|---|---|---|
| 시작 화면 | 진입 장벽 제거 | 영상 선택 버튼, 드래그 영역, 최근 프로젝트 목록, 프로젝트 파일 열기 |
| 편집 화면 | 컷편집 | 미리보기, 타임라인, 도구 버튼, 클립 속성 패널 |
| 내보내기 화면 | 출력 설정 | 화면비·해상도·화질·목표 용량, 예상 용량, 진행률 |
| 프로젝트 목록 | 이어 작업 | 썸네일, 수정일, 용량, 삭제, 파일로 내보내기 |

**데스크톱 레이아웃**

상단 미리보기 + 우측 속성 패널, 하단 전체 폭 타임라인. 클립 끝을 드래그해 트림, 클립을 드래그해
순서 변경.

**모바일 세로 레이아웃 (필수)**

- 상단 미리보기, 하단 가로 스크롤 타임라인, 최하단 큰 도구 버튼 바
- 재생헤드는 화면 가로 중앙에 고정, 타임라인을 손가락으로 밀어 위치 이동
- 핀치로 타임라인 확대·축소(최대 프레임 단위)
- 모든 주 조작 버튼 최소 44×44pt
- 분할·삭제·빈 화면 추가·되돌리기를 버튼 한 번으로 실행
- 클립 트림은 클립을 탭해 선택한 뒤 좌우 핸들을 드래그, 확대 시 프레임 단위 조정

**접근성·기타**

- 키보드 단축키: `Space` 재생/정지, `S` 분할, `Delete` 삭제, `←/→` 1프레임 이동, `Ctrl+Z` 되돌리기
- 모든 상태 변화에 텍스트 안내 제공(색상만으로 상태를 전달하지 않음)
- 되돌리기·다시 실행 50단계
- 한국어 기본, 문자열은 교체 가능한 구조로 분리

## 7. 기능 요구사항

| ID | 우선순위 | 요구사항 | AC |
|---|---|---|---|
| FR-001 | 필수 | 사용자는 로그인 없이 첫 화면에서 즉시 영상을 불러올 수 있다. | AC-001 |
| FR-002 | 필수 | 사용자는 영상 파일 여러 개와 이미지 파일을 한 타임라인에 추가할 수 있다. | AC-002, AC-003 |
| FR-003 | 필수 | 브라우저가 디코딩할 수 없는 파일은 거부 사유를 표시하고, 나머지 파일 추가는 계속 진행한다. | AC-004 |
| FR-004 | 필수 | 사용자는 재생헤드 위치에서 클립을 두 개로 분할할 수 있다. | AC-005 |
| FR-005 | 필수 | 사용자는 클립의 시작·끝을 조절해 길이를 줄일 수 있고, 원본 길이 범위 안에서 다시 늘릴 수 있다. | AC-006, AC-007 |
| FR-006 | 필수 | 사용자는 클립을 삭제할 수 있으며, 삭제 후 뒤 클립이 빈틈 없이 앞으로 당겨진다. | AC-008 |
| FR-007 | 필수 | 사용자는 클립의 순서를 바꿀 수 있다. | AC-009 |
| FR-008 | 필수 | 사용자는 지정한 길이와 색의 빈 화면을 타임라인 임의 위치에 삽입할 수 있다. | AC-010 |
| FR-009 | 필수 | 이미지 클립은 사용자가 지정한 표시 시간(기본 3초)만큼 재생된다. | AC-011 |
| FR-010 | 필수 | 사용자는 클립별로 음소거하거나 볼륨을 0~200% 범위에서 조절할 수 있다. | AC-012 |
| FR-011 | 필수 | 이미지·빈 화면 구간은 무음으로 출력되며 전체 오디오 길이가 영상 길이와 일치한다. | AC-013 |
| FR-012 | 필수 | 미리보기는 타임라인 편집 결과를 그대로 재생한다. | AC-014 |
| FR-013 | 필수 | 사용자는 되돌리기·다시 실행을 최소 50단계 사용할 수 있다. | AC-015 |
| FR-014 | 필수 | 사용자는 출력 화면비를 16:9 / 9:16 / 1:1 / 원본 유지 중에서 고를 수 있다. | AC-016 |
| FR-015 | 필수 | 화면비가 다른 소스는 사용자가 고른 방식(흐린 배경 채우기 / 여백 채우기 / 잘라 채우기)으로 정규화된다. 기본값은 흐린 배경 채우기다. | AC-017, AC-045 |
| FR-016 | 필수 | 사용자는 출력 해상도를 2160p / 1440p / 1080p / 720p / 480p 중에서 고를 수 있다. | AC-018 |
| FR-017 | 필수 | 사용자는 화질을 높음·보통·낮음 중에서 고를 수 있고, 각 설정의 예상 최대 용량이 실시간 표시된다. | AC-019 |
| FR-018 | 필수 | 사용자는 목표 용량(MB)을 직접 입력할 수 있고, 시스템은 그 이하가 되도록 비트레이트를 역산해 인코딩한다. | AC-020 |
| FR-019 | 필수 | 결과가 목표 용량을 초과하면 비트레이트를 낮춰 1회만 자동 재인코딩하고, 그래도 초과하면 해상도 낮춤을 제안한다. | AC-021, AC-022 |
| FR-020 | 필수 | 내보내기는 MP4(H.264 영상 / AAC 오디오)로 출력한다. | AC-023 |
| FR-021 | 필수 | 내보내기 중 진행률과 남은 예상 시간을 표시하고, 사용자는 취소할 수 있다. | AC-024 |
| FR-022 | 필수 | 영상·이미지 파일과 편집 내용은 어떠한 경우에도 외부 서버로 전송되지 않는다. | AC-025 |
| FR-023 | 필수 | 편집 내용은 조작 후 자동으로 브라우저에 저장되고, 다시 접속하면 프로젝트 목록에서 복원할 수 있다. | AC-026 |
| FR-024 | 필수 | 사용자는 프로젝트를 `.cutcap` 파일로 내보내고 다시 불러올 수 있다. | AC-027, AC-028 |
| FR-025 | 필수 | `.cutcap`를 불러올 때 원본 파일이 없으면 어떤 파일이 필요한지 목록으로 알리고 재선택을 요청한다. | AC-029 |
| FR-026 | 필수 | 저장공간이 부족하면 자동 저장 전에 경고하고 프로젝트 파일 내보내기를 권유한다. | AC-030 |
| FR-027 | 필수 | iOS Safari에서는 7일 미접속 시 자동 저장분이 삭제될 수 있음을 고지하고 홈 화면 추가를 안내한다. | AC-031 |
| FR-028 | 필수 | WebCodecs를 지원하지 않는 브라우저에서는 ffmpeg.wasm 경로로 자동 전환하고 소요 시간 증가를 고지한다. | AC-032 |
| FR-029 | 필수 | 모바일에서 재생헤드는 화면 중앙에 고정되고 타임라인을 밀어 위치를 옮긴다. | AC-033 |
| FR-030 | 필수 | 내보내기 진행 중 페이지를 벗어나려 하면 경고한다. | AC-034 |
| FR-031 | 선택 | 사용자는 프로젝트 이름을 바꾸고 목록에서 삭제할 수 있다. | AC-035 |
| FR-032 | 선택 | 앱은 PWA로 설치 가능하며 오프라인에서 실행된다. | AC-036 |
| FR-033 | 필수 | 사용자는 자막 항목(텍스트, 시작·종료 시각)을 추가·편집·삭제할 수 있다. | AC-037, AC-038 |
| FR-034 | 필수 | 재생헤드 위치에서 자막을 추가할 수 있고, 입력 즉시 미리보기에 반영된다. | AC-039 |
| FR-035 | 필수 | 자막은 클립에 종속되어, 클립을 옮기거나 분할·트림해도 해당 구간과 함께 움직인다. | AC-040, AC-041 |
| FR-036 | 필수 | 사용자는 자막 스타일(글자 크기, 글자색, 외곽선 또는 배경상자, 세로 위치)을 프로젝트 전체에 일괄 적용할 수 있다. | AC-042 |
| FR-037 | 필수 | 내보내기 시 자막이 영상 프레임에 렌더링되어 어떤 플레이어에서도 보인다. | AC-043 |
| FR-038 | 선택 | 사용자는 자막을 `.srt` 파일로 내보낼 수 있다. | AC-044 |
| FR-039 | 후속 | 자동 음성인식 자막 | — |
| FR-040 | 후속 | 배경음악 1트랙 추가 및 볼륨 밸런스 | — |
| FR-041 | 후속 | 텍스트 오버레이·스티커 | — |
| FR-042 | 후속 | 클립 속도 조절 | — |

## 8. 데이터 모델

모든 데이터는 사용자 기기에만 저장됩니다. 서버 데이터베이스는 없습니다.

**Project**

| 필드 | 자료형 | 필수 | 제약·설명 |
|---|---|---|---|
| id | string(uuid) | 예 | 생성 시 발급 |
| name | string | 예 | 기본값 "제목 없는 프로젝트", 최대 100자 |
| createdAt / updatedAt | ISO datetime | 예 | |
| timeline | TimelineItem[] | 예 | 순서가 곧 재생 순서 |
| sources | MediaSource[] | 예 | 참조되는 원본 목록 |
| subtitles | Subtitle[] | 예 | 자막 목록. 비어 있을 수 있음 |
| subtitleStyle | SubtitleStyle | 예 | 프로젝트 전체 자막 스타일 |
| exportSetting | ExportSetting | 예 | 마지막 사용 설정 |
| thumbnail | Blob | 아니오 | 목록 표시용, 320px 이하 |
| schemaVersion | number | 예 | `.cutcap` 호환성 판단용 |

**MediaSource**

| 필드 | 자료형 | 필수 | 제약·설명 |
|---|---|---|---|
| id | string(uuid) | 예 | |
| kind | `"video"` \| `"image"` | 예 | |
| fileName / fileSize / lastModified | string / number / number | 예 | `.cutcap` 재연결 시 대조용 |
| duration | number(초) | video만 | |
| width / height / fps | number | video만 | |
| hasAudio | boolean | video만 | |
| storageKey | string | 아니오 | OPFS 내 경로. 프로젝트 파일에는 포함하지 않음 |

**TimelineItem**

| 필드 | 자료형 | 필수 | 제약·설명 |
|---|---|---|---|
| id | string(uuid) | 예 | |
| type | `"video"` \| `"image"` \| `"blank"` | 예 | |
| sourceId | string | blank 외 필수 | MediaSource 참조 |
| inPoint / outPoint | number(초) | video 필수 | 0 ≤ in < out ≤ source.duration |
| duration | number(초) | image·blank 필수 | 0.1 이상, 기본 image 3초 / blank 2초 |
| volume | number | video 필수 | 0.0~2.0, 기본 1.0 |
| muted | boolean | video 필수 | 기본 false |
| color | string(hex) | blank 필수 | 기본 `#000000` |

**ExportSetting**

| 필드 | 자료형 | 필수 | 제약·설명 |
|---|---|---|---|
| aspectRatio | `"16:9"` \| `"9:16"` \| `"1:1"` \| `"source"` | 예 | 기본 `source` |
| fitMode | `"contain"` \| `"cover"` | 예 | 여백 채우기 / 잘라 채우기, 기본 `contain` |
| resolution | `2160`\|`1440`\|`1080`\|`720`\|`480` | 예 | 기본 `1080` |
| quality | `"high"`\|`"medium"`\|`"low"` | 예 | 기본 `medium` |
| targetSizeMB | number \| null | 아니오 | 지정 시 quality보다 우선, 1 이상 |
| fps | `30` \| `60` \| `"source"` | 예 | 기본 `source`, 상한 60 |

**Subtitle**

자막은 타임라인 절대 시간이 아니라 **클립에 종속**됩니다. 절대 시간에 묶으면 클립을
삭제하거나 순서를 바꾸는 순간 모든 자막이 어긋나 처음부터 다시 맞춰야 하기 때문입니다.

| 필드 | 자료형 | 필수 | 제약·설명 |
|---|---|---|---|
| id | string(uuid) | 예 | |
| clipId | string | 예 | 종속된 TimelineItem 참조. 해당 클립 삭제 시 함께 삭제 |
| startInClip / endInClip | number(초) | 예 | 클립 원본 기준 상대 시각. 0 ≤ start < end |
| text | string | 예 | 최대 200자, 줄바꿈 허용(최대 3줄) |

동작 규칙: 클립을 분할하면 경계에 걸친 자막도 경계에서 나뉩니다. 트림으로 클립의
`inPoint`~`outPoint` 밖으로 나간 자막은 표시·출력되지 않지만 삭제하지는 않아,
트림을 되돌리면 다시 나타납니다.

**SubtitleStyle** (프로젝트당 1개)

| 필드 | 자료형 | 필수 | 제약·설명 |
|---|---|---|---|
| fontScale | number | 예 | 출력 높이 대비 글자 크기 비율. 0.02~0.10, 기본 0.045 |
| color | string(hex) | 예 | 기본 `#FFFFFF` |
| background | `"outline"` \| `"box"` \| `"none"` | 예 | 기본 `outline` |
| backgroundOpacity | number | 예 | `box`일 때 0.0~1.0, 기본 0.6 |
| verticalPosition | number | 예 | 아래에서부터의 비율 0.02~0.5, 기본 0.08 |

**보관·삭제**

- 자동 저장본은 사용자가 프로젝트를 삭제할 때까지 유지하며, 목록에서 직접 삭제할 수 있습니다.
- 브라우저 저장소를 비우면 자동 저장본이 함께 삭제되며 복구할 수 없습니다(사용자에게 고지).
- `.cutcap` 파일에는 편집 정보와 원본 파일의 이름·크기·수정일만 담고 **영상 데이터는 포함하지
  않습니다**(파일 크기 수십 KB 유지).

## 9. 연동과 AI

| 항목 | 내용 |
|---|---|
| 외부 API | 없음 |
| AI 기능 | 없음 (MVP에서 AI를 쓰지 않습니다. 컷 편집 판단은 전적으로 사용자 몫이며, AI 자동 편집은 잘못된 구간을 잘라낼 위험이 커 범위에서 제외했습니다.) |
| 사용하는 브라우저 API | WebCodecs(`VideoEncoder`/`VideoDecoder`), `ffmpeg.wasm`(폴백 및 먹싱), OPFS, IndexedDB, File System Access(데스크톱 저장 시), Canvas/OffscreenCanvas, Web Worker, Storage API |
| 호스팅 | Vercel 정적 배포. 애플리케이션 자산 전송 외의 네트워크 통신 없음 |
| 분석 도구 | MVP에서는 도입하지 않음 (미정 → 도입 시 영상 내용·파일명을 포함하지 않는 익명 지표만 허용) |

## 10. 보안과 개인정보

| 항목 | 내용 |
|---|---|
| 계정·인증 | 없음. 서버에 사용자 데이터가 존재하지 않으므로 인증 대상이 없습니다. |
| 개인정보 수집 | 없음. 영상·이미지·파일명·편집 내용 모두 기기를 벗어나지 않습니다. |
| 전송 구간 | 앱 자산은 HTTPS(TLS)로 전송. 사용자 미디어는 전송 자체가 없습니다. |
| 저장 | OPFS/IndexedDB에 평문 저장. 기기 소유자만 접근 가능한 오리진 전용 영역이며, 서버로 나가지 않으므로 필드 암호화를 적용하지 않습니다. 대신 "기기를 공유해 쓰면 다른 사용자가 열람할 수 있다"는 점을 프로젝트 목록 화면에 고지합니다. |
| 비밀키 | 없음(외부 API 미사용) |
| 로그 | 서버 로그 없음. 클라이언트 오류 메시지에 파일명·경로를 포함하지 않습니다. |
| 필수 보안 헤더 | `Cross-Origin-Opener-Policy: same-origin`, `Cross-Origin-Embedder-Policy: require-corp` (SharedArrayBuffer 활성화용), `Content-Security-Policy`로 외부 스크립트 차단 |
| 파일 처리 | 사용자가 고른 파일만 `File` 객체로 읽고, 디코딩 실패 시 예외를 잡아 앱 전체가 멈추지 않게 합니다. |
| 삭제 | 프로젝트 삭제 시 OPFS의 원본 사본과 IndexedDB 레코드를 함께 제거합니다. |

## 11. 비기능 요구사항

| 항목 | 기준 | 검증 방법 |
|---|---|---|
| 최초 로딩 | 4G 환경에서 3초 이내 첫 화면 표시. ffmpeg.wasm은 내보내기 시점에 지연 로딩 | Lighthouse 측정 |
| 편집 반응성 | 분할·삭제·순서 변경은 200ms 이내 화면 반영 | 성능 프로파일 |
| 타임라인 스크롤 | 60fps 유지(클립 30개 기준) | 모바일 실기기 측정 |
| 내보내기 성능 (제안) | 데스크톱 1080p 3분 영상 WebCodecs 경로 90초 이내 | 기준 영상으로 측정 |
| 입력 한계 (가정) | 파일당 최대 2GB, 프로젝트 총 길이 최대 60분, 클립 최대 100개. 초과 시 사전 경고 | 경계값 테스트 |
| 저사양 모바일 | 720p 3분 영상 내보내기 성공 | 실기기 테스트 |
| 지원 브라우저 | 6절 목록. 미지원 브라우저는 진입 시 안내 | 브라우저 매트릭스 테스트 |
| 오프라인 | 최초 방문 이후 오프라인에서 편집·내보내기 가능 | 기내 모드 테스트 |
| 접근성 | 키보드만으로 편집·내보내기 완료 가능, 대비 4.5:1 이상 | 수동 점검 |

## 12. 인수 기준

| ID | 사전 조건 | 행동 | 기대 결과 |
|---|---|---|---|
| AC-001 | 앱에 처음 접속 | 첫 화면에서 영상 파일 1개 선택 | 로그인·가입 없이 편집 화면으로 진입하고 타임라인에 클립 1개가 표시된다 |
| AC-002 | 편집 화면 | 영상 3개를 추가 | 타임라인에 선택 순서대로 3개 클립이 이어져 배치되고 총 길이가 각 길이의 합과 같다 |
| AC-003 | 편집 화면 | JPG 이미지 1개를 추가 | 기본 3초 길이의 이미지 클립이 추가된다 |
| AC-004 | 편집 화면 | 브라우저가 못 읽는 코덱 파일 1개와 정상 영상 1개를 함께 선택 | 정상 영상은 추가되고, 실패 파일은 파일명과 사유가 표시된다 |
| AC-005 | 10초 클립, 재생헤드 4초 | 분할 실행 | 0~4초, 4~10초 두 클립으로 나뉘고 총 길이는 10초 그대로다 |
| AC-006 | 10초 클립 | 끝 지점을 7초로 조절 | 클립 길이가 7초가 되고 미리보기가 7초에서 끝난다 |
| AC-007 | 위 상태 | 끝 지점을 다시 10초로 조절 | 원본 길이인 10초까지 복원되고, 10초를 넘겨서는 늘어나지 않는다 |
| AC-008 | 클립 3개(각 5초) | 두 번째 클립 삭제 | 총 길이가 10초가 되고 세 번째 클립이 5초 지점부터 시작하며 사이에 빈 구간이 없다 |
| AC-009 | 클립 3개 | 세 번째 클립을 맨 앞으로 이동 | 재생 순서가 3-1-2로 바뀌고 미리보기도 동일하게 재생된다 |
| AC-010 | 클립 2개 | 두 클립 사이에 검정 빈 화면 2초 삽입 | 해당 구간이 검정 화면·무음으로 재생되고 총 길이가 2초 늘어난다 |
| AC-011 | 이미지 클립 | 표시 시간을 5초로 변경 | 이미지가 5초간 표시되고 총 길이에 반영된다 |
| AC-012 | 소리 있는 클립 | 음소거 적용 후 내보내기 | 해당 구간만 무음이고 다른 클립 소리는 유지된다 |
| AC-013 | 영상+이미지+빈 화면 혼합 | 내보내기 | 출력 파일의 오디오 길이가 영상 길이와 같고, 이미지·빈 화면 구간은 무음이며 뒤 구간의 소리가 밀리지 않는다 |
| AC-014 | 분할·삭제·순서 변경을 마친 상태 | 처음부터 재생 | 내보내기 결과와 동일한 순서·길이로 재생된다 |
| AC-015 | 편집 10회 수행 | 되돌리기 10회 | 편집 전 상태로 정확히 복원되고, 다시 실행으로 되돌아온다 |
| AC-016 | 16:9 원본 | 화면비 9:16 선택 후 내보내기 | 출력 영상의 가로세로 비가 9:16이다 |
| AC-017 | 16:9 원본, 9:16 출력 | 여백 채우기 선택 | 위아래에 검은 여백이 생기고 영상이 잘리지 않는다. 잘라 채우기 선택 시 여백 없이 좌우가 잘린다 |
| AC-045 | 세로 영상과 가로 영상이 섞인 타임라인 | 기본값(흐린 배경 채우기)으로 내보내기 | 화면비가 맞지 않는 구간이 검은 여백 없이 채워지고, 원본은 잘리지 않는다. 채운 부분은 같은 화면을 흐리게 키운 것이며 미리보기도 같게 보인다 |
| AC-018 | 1080p 원본 | 720p 선택 후 내보내기 | 출력 영상 세로 해상도가 720이다 |
| AC-019 | 편집 완료 | 화질을 높음↔낮음으로 변경 | 예상 용량 표시가 즉시 갱신되고, 실제 결과가 표시값을 넘지 않는다. 표시값은 상한이며 장면이 단순하면 더 작게 나온다 |
| AC-020 | 3분 영상 | 목표 용량 20MB 입력 후 내보내기 | 결과 파일이 20MB 이하다 |
| AC-021 | 1회차 결과가 목표 초과 | 자동 재인코딩 수행 | 재인코딩은 정확히 1회만 실행되고 화면에 재시도 중임이 표시된다 |
| AC-022 | 재인코딩 후에도 초과 | 결과 화면 확인 | 목표 미달성 사실과 해상도 낮춤 제안이 표시되며, 생성된 파일은 그대로 저장할 수 있다 |
| AC-023 | 내보내기 완료 | 결과 파일을 확인 | 확장자가 `.mp4`이고 Windows·macOS·iOS·Android 기본 플레이어에서 영상·소리 모두 재생된다 |
| AC-024 | 내보내기 진행 중 | 취소 버튼 클릭 | 5초 이내 중단되고 편집 화면으로 돌아가며 편집 내용이 유지된다 |
| AC-025 | 개발자 도구 네트워크 탭 열림 | 파일 추가부터 내보내기까지 전 과정 수행 | 사용자 미디어를 담은 업로드 요청이 한 건도 발생하지 않는다 |
| AC-026 | 편집 후 탭을 닫음 | 같은 브라우저로 재접속 | 프로젝트 목록에 해당 프로젝트가 있고, 열면 편집 상태가 그대로 복원된다 |
| AC-027 | 편집 상태 | `.cutcap` 내보내기 | 수 KB~수십 KB 크기 파일이 저장되고 영상 데이터를 포함하지 않는다 |
| AC-028 | 다른 기기·브라우저 | `.cutcap`를 열고 원본 파일 재선택 | 클립 순서·분할·트림·볼륨 설정이 저장 시점과 동일하게 복원된다 |
| AC-029 | 원본 중 1개가 없는 상태 | `.cutcap` 열기 | 어떤 파일이 필요한지 파일명·크기로 안내하고, 해당 파일만 다시 고르면 복원된다 |
| AC-030 | 저장공간이 거의 찬 기기 | 큰 영상 추가 | 자동 저장 시도 전에 경고가 표시되고 프로젝트 파일 내보내기를 권유한다. 앱이 멈추지 않는다 |
| AC-031 | iOS Safari 접속 | 프로젝트를 처음 저장 | 7일 미접속 시 삭제 가능성 고지와 홈 화면 추가 안내가 1회 표시된다 |
| AC-032 | WebCodecs 미지원 브라우저 | 내보내기 실행 | ffmpeg.wasm 경로로 정상 완료되고, 시작 전 소요 시간 증가 안내가 표시된다 |
| AC-033 | 스마트폰 세로 화면 | 타임라인을 좌우로 미는 조작 | 재생헤드는 중앙에 고정된 채 타임라인이 움직이고, 핀치로 확대하면 프레임 단위로 위치를 맞출 수 있다 |
| AC-034 | 내보내기 진행 중 | 탭 닫기 시도 | 브라우저 확인 대화상자가 표시된다 |
| AC-035 | 프로젝트 2개 존재 | 하나의 이름 변경 후 다른 하나 삭제 | 이름이 갱신되고 삭제한 프로젝트와 그 원본 사본이 저장소에서 제거된다 |
| AC-036 | 앱 1회 방문 후 오프라인 | 앱 재실행 | 첫 화면이 정상 표시되고 저장된 프로젝트를 열어 편집·내보내기할 수 있다 |
| AC-037 | 10초 클립 | 2~5초 구간에 자막 "안녕하세요" 추가 | 재생 시 2초에 나타나 5초에 사라지고, 그 밖의 구간에는 보이지 않는다 |
| AC-038 | 자막 1개 존재 | 텍스트 수정 후 삭제 | 수정이 즉시 반영되고, 삭제하면 미리보기와 출력에서 모두 사라진다 |
| AC-039 | 재생헤드 4초 | 자막 추가 버튼 클릭 | 4초에서 시작하는 자막이 생성되고 입력창에 바로 커서가 놓인다 |
| AC-040 | 클립 3개, 세 번째 클립에 자막 존재 | 세 번째 클립을 맨 앞으로 이동 | 자막이 클립을 따라 이동해 해당 영상 구간에서 그대로 표시된다 |
| AC-041 | 자막이 걸친 지점에서 클립 분할 | 분할 실행 후 확인 | 자막이 경계에서 두 개로 나뉘고 표시 시점은 분할 전과 같다. 이후 트림으로 잘려나간 구간의 자막은 보이지 않다가, 트림을 되돌리면 다시 나타난다 |
| AC-042 | 자막 여러 개 존재 | 글자 크기와 세로 위치 변경 | 모든 자막에 동시에 적용되고 미리보기에 즉시 반영된다 |
| AC-043 | 자막이 있는 프로젝트 | 내보내기 후 결과 파일 재생 | 자막이 영상에 새겨져 자막 설정이 없는 기본 플레이어에서도 보인다 |
| AC-044 | 자막이 있는 프로젝트 | `.srt` 내보내기 | 표준 SRT 형식이며 타임코드가 출력 영상의 자막 표시 시점과 일치한다 |

## 13. 기술 구성과 개발 순서

**확정**

| 영역 | 선택 |
|---|---|
| 언어·프레임워크 | TypeScript + React |
| 빌드 | Vite |
| 배포 | Vercel 정적 배포 (`vercel.json`에 COOP/COEP 헤더) |
| 인코딩 | WebCodecs 우선, 미지원 시 `@ffmpeg/ffmpeg`(ffmpeg.wasm) 폴백 |
| 먹싱 | MP4 먹서(WebCodecs 경로), ffmpeg 경로는 자체 먹싱 |
| 저장 | OPFS(원본 사본) + IndexedDB(프로젝트 메타) |
| 서버 | 없음 |

**제안 (개발 단계에서 조정 가능)**

- 상태 관리: Zustand (타임라인 상태 + 되돌리기 스택)
- 스타일: Tailwind CSS
- 인코딩·디코딩은 Web Worker에서 수행해 UI 프리징 방지
- PWA: `vite-plugin-pwa`

**개발 순서 (각 단계는 독립적으로 동작 확인 가능)**

| 단계 | 내용 | 관련 FR | 완료 판단 |
|---|---|---|---|
| 1 | 프로젝트 골격, Vercel 배포, COOP/COEP 헤더, 환경 진단 모듈 | — | 배포 URL에서 `crossOriginIsolated === true` |
| 2 | 내보내기 최소 검증: 영상 1개를 MP4로 재인코딩 (WebCodecs) | FR-020 | 결과 MP4가 기본 플레이어에서 영상·소리 재생 |
| 3 | 파일 불러오기 + 메타데이터 추출 + 타임라인 표시 | FR-001~003 | AC-001~004 |
| 4 | 타임라인 편집(분할·트림·삭제·순서·되돌리기) | FR-004~007, 013 | AC-005~009, 015 |
| 5 | 빈 화면·이미지 클립, 볼륨·음소거 | FR-008~011 | AC-010~013 |
| 6 | 미리보기 재생 엔진 | FR-012 | AC-014 |
| 7 | 자막 트랙: 입력·타이밍·클립 종속·스타일·미리보기 | FR-033~036 | AC-037~042 |
| 8 | 전체 타임라인 내보내기 (혼합 소스 + 자막 굽기 + `.srt`) | FR-021, 030, 037, 038 | AC-013, 023, 024, 034, 043, 044 |
| 9 | 출력 설정 (화면비·정규화·해상도·화질) | FR-014~017 | AC-016~019, AC-045 |
| 10 | 목표 용량 인코딩 + 1회 재시도 | FR-018, 019 | AC-020~022 |
| 11 | 자동 저장·복원, 프로젝트 목록 | FR-023, 026, 031 | AC-026, 030, 035 |
| 12 | `.cutcap` 내보내기·불러오기·재연결 | FR-024, 025 | AC-027~029 |
| 13 | 모바일 세로 레이아웃 | FR-029 | AC-033 |
| 14 | ffmpeg.wasm 폴백, iOS 고지, PWA | FR-027, 028, 032 | AC-031, 032, 036 |
| 15 | 성능·저사양 기기 검증 | 11절 | 11절 기준 |

## 14. 결정·가정·미결정·위험

**확정 결정과 근거**

| 결정 | 근거 |
|---|---|
| 브라우저 전용 처리 | 무료 유지와 영상 비전송을 동시에 만족하는 유일한 구조 |
| 저장 이중화(자동 저장 + 파일) | iOS Safari의 7일 저장소 삭제 정책으로 자동 저장만으로는 작업물 유실이 발생 |
| MP4 단일 출력 | 검증 경로를 줄이고 재생 호환성 문제를 원천 차단 |
| 오디오는 원본만 | 믹싱은 싱크 어긋남의 주원인이며 컷편집 가치와 분리됨 |
| 모바일 전용 레이아웃 | 반응형 축소로는 좁은 화면에서 트림 조작이 성립하지 않음 |
| 자막을 클립에 종속 | 절대 시간 기준이면 클립 삭제·순서 변경 시 모든 자막이 어긋나 재작업이 발생 |
| 자막을 영상에 굽기 | `.srt`만 내보내면 카카오톡·업무포털 공유 시 자막이 보이지 않음 |

**가정 (검증 필요)**

- 입력 상한 파일당 2GB / 총 60분 / 클립 100개는 대표 기기 테스트로 조정합니다.
- 내보내기 성능 목표(1080p 3분 90초)는 기준 기기 확정 후 재측정합니다.
- 예상 용량 오차 ±20%는 1단계 구현 후 실측으로 조정합니다.

**미결정 (착수를 막지 않음)**

- 익명 사용량 측정 도입 여부
- 다국어 지원 시점
- 도메인 및 서비스 명칭 최종안

**위험과 대응**

| 위험 | 영향 | 대응 |
|---|---|---|
| 저사양 모바일에서 메모리 부족으로 인코딩 실패 | 핵심 기능 실패 | 해상도·길이 기준 사전 경고, 실패 시 구체적 대안 안내, 청크 단위 처리 |
| ffmpeg.wasm 폴백 경로가 지나치게 느림 | 사용자 이탈 | 예상 시간 사전 고지, 해상도 낮춤 권장, 취소 가능 |
| OPFS 용량 한계로 원본 사본 저장 실패 | 자동 저장 불가 | 용량 사전 확인, 실패 시 프로젝트 파일 내보내기로 유도 |
| iOS Safari 저장소 삭제 | 작업물 유실 | 고지 + 홈 화면 추가 안내 + 파일 내보내기 상시 노출 |
| 오디오·영상 싱크 어긋남 | 결과물 불량 | 6단계에서 혼합 타임라인 싱크 테스트를 필수 관문으로 설정 |
| COOP/COEP 적용 후 외부 리소스 차단 | 빌드 실패 | 외부 스크립트·폰트를 쓰지 않고 모든 자산을 자체 호스팅 |

## 15. 개발 도구 전달 프롬프트

```
CutCap 웹 앱을 PRD.md(v1.2) 기준으로 구현해 주세요.

[제품] 설치·로그인 없이 브라우저에서 영상을 자르고 이어 붙여 MP4로 내보내는 무료 웹 편집기.

[확정 기술]
- TypeScript + React + Vite, Vercel 정적 배포
- 서버 코드·데이터베이스·외부 API 없음
- 인코딩: WebCodecs 우선, 미지원 시 ffmpeg.wasm 폴백. 모두 Web Worker에서 실행
- 저장: OPFS(원본 사본) + IndexedDB(프로젝트 메타), .cutcap 파일 내보내기·불러오기
- vercel.json에 COOP: same-origin, COEP: require-corp 헤더 설정 (SharedArrayBuffer 필수)

[필수 범위] PRD 7절의 FR-001~FR-038 전부.
[제외 범위] 자동 음성인식 자막, 배경음악·오디오 믹싱, 텍스트 오버레이·스티커,
           화면 전환·필터, 계정·클라우드, MP4 외 출력 포맷, 서버 인코딩.
           요청 없이 추가하지 마세요.

[반드시 지킬 규칙]
1. 사용자 미디어 파일은 어떤 경우에도 네트워크로 전송하지 않습니다.
2. 출력은 MP4(H.264/AAC) 하나로 고정합니다.
3. 목표 용량 초과 시 재인코딩은 정확히 1회만 수행합니다.
4. 모바일은 별도 세로 레이아웃이며 재생헤드를 화면 중앙에 고정합니다.
5. 자막은 절대 시간이 아니라 클립에 종속시킵니다.
6. 외부 CDN 스크립트·폰트를 쓰지 않고 모든 자산을 자체 호스팅합니다.

[허용된 가정] 입력 상한(파일당 2GB / 총 60분 / 클립 100개)과 성능 목표는 구현 후
실측으로 조정 가능합니다. 상태 관리·스타일 라이브러리는 제안일 뿐 변경 가능합니다.

[착수 차단 미결정] 없음.

[작업 방식]
- PRD 13절의 개발 순서 1~15단계를 그 순서대로 진행하고, 한 번에 한 단계씩 처리합니다.
- 각 단계를 시작하기 전에 구현 계획을 먼저 제시하고 승인을 받은 뒤 코드를 작성합니다.
- 각 단계를 마치면 해당 단계의 AC ID를 기준으로 무엇을 어떻게 확인했는지 보고합니다.
- 1단계 완료 기준은 배포된 페이지에서 crossOriginIsolated가 true인 것입니다.
- 기존 코드의 대폭 변경이나 삭제가 필요하면 변경안을 먼저 제시하고 승인을 받습니다.

[첫 단계] 1단계(프로젝트 골격 + Vercel 배포 + COOP/COEP 헤더)부터 시작하세요.
```
