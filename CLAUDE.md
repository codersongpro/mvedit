# CutCap 작업 규칙

브라우저 안에서만 도는 영상 컷편집·자막 웹앱. 서버로 보내는 데이터가 없다.

## 제작자 표기

이 프로젝트(그리고 사용자의 다른 산출물)에 만든 사람을 적을 때는 항상 이렇게 쓴다.

```
송동석 · Teacher / Data Analystic / App developer
```

## 작업 방식

- PRD.md 가 기준이다. 기능을 바꾸면 해당 FR/AC 도 함께 고치고, 결정이 바뀌면
  14절 결정 표에 근거를 남긴다.
- 한국어로 쓴다 — 주석, 커밋 메시지, 화면 문구 모두.
- 주석은 "무엇"이 아니라 "왜"를 적는다. 코드를 읽으면 아는 것은 적지 않는다.

## 검증

```bash
npm run build   # 타입 검사 + 빌드 + 서비스 워커 캐시 목록 주입
npm test        # 11개 스위트 전부 (Playwright, 실제 UI 로 확인)
```

- 모든 검증은 **프로덕션 빌드를 띄워** 돌린다. 코드를 고쳤으면 빌드부터 한다.
- 제품 코드에 시험용 분기를 두지 않는다. 필요한 경우 테스트 쪽에서
  브라우저 API 를 바꿔 끼운다(`navigator.storage.estimate`, WebCodecs 제거 등).
- `page.waitForFunction` 에 async 함수를 넘기지 않는다. 돌아온 Promise 자체가
  참으로 취급돼 첫 판에 통과해 버린다. Node 쪽에서 `page.evaluate` 로 돌린다.

## 코드 스타일

- 포맷은 `.prettierrc` (작은따옴표, 세미콜론 없음, 100칸). 설정 없이 prettier 를
  돌리면 저장소 전체가 재포맷되므로 항상 이 설정을 쓴다.
- 컨테이너 Chromium 에는 H.264·AAC 인코더가 없다. 자동 검증은 VP9/Opus 로
  돌아가며, MP4 결과물은 사람이 실기기에서 확인해야 한다.
